---
import Hero from "../Hero.astro";
import BlockRenderer from "../BlockRenderer.astro";

type Props = {
  data: any;
};

const { data } = Astro.props;

const projectHero= data.projectHero.details;
const projectContent = data.projectContent.content;
---

<Hero data={projectHero} />


<article class="max-w-[64rem] flex flex-col mx-auto size-full">
  <BlockRenderer blocks={projectContent} />
</article>
