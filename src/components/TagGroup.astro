---
import Tag from '../components/Tag.astro'

const { tags } = Astro.props
---

<ul class="flex self-baseline">
  {tags.map((item: any) => <Tag link={item.link} label={item.title} />)}
</ul>

<style>
  ul {
    gap: 0.225em;
  }
</style>